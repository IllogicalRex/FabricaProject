export class Project {

    constructor(
        // tslint:disable-next-line:variable-name
        public pro_ID?: number,
        public name?: string,
        public startDate?: Date,
        public endDate?: Date,
        public proyectLeader?: string
    ) {    }
}
